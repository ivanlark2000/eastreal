[SQL]
site_id-price.part_1=SELECT s.site_id, b.n_price FROM inf_sys s INNER JOIN(
        SELECT f_flat, n_price::money::numeric::float8
        FROM mn_ads_price
        WHERE link IN (
            SELECT MAX(link) 
            FROM mn_ads_price
            GROUP BY f_flat 
        )
    ) b ON b.f_flat = s.f_flat WHERE s.site_id IS NOT NULL AND f_city = 
