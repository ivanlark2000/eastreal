--функция для расчета пропорции
CREATE OR REPLACE FUNCTION count_proportion(dist1 smallint, dist2 smallint, dist3 smallint, w1 numeric, w2 numeric)
RETURNS numeric
AS
$BODY$

BEGIN

            
END;
$BODY$
LANGUAGE plpgsql